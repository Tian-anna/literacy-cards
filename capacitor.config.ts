import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yourcompany.literacycard',
  appName: 'LiteracyCardApp',
  webDir: 'dist'
};

export default config;
